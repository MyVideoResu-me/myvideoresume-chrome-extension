/**
 * hired.video Chrome Extension - Job Seeker Service Worker
 *
 * Extension-specific handlers appended after service-worker-base.js
 * by the build script. The base handles JWT refresh, auth sync,
 * URL-change forwarding, jobDetected forwarding, and HTML retrieval.
 *
 * Currently no jobseeker-specific handlers are needed — all logic
 * lives in the shared base.
 */
